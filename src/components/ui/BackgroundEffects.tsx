"use client";
import StarsBackground from "@/components/ui/StarsBackgroundClient";
import ShootingStars from "@/components/ui/ShootingStarsClient";

const ClientLayoutWrapper = () => {
    return (
        <>
            <StarsBackground />
            <ShootingStars />
        </>
    );
};

export default ClientLayoutWrapper;
